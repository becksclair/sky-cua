## Kate Computer Use

Kate exposes a fairly decent accessibility tree on KDE. Prefer semantic reads of focused controls, values, text readback, and window titles, but keep editor-content replacement keyboard-driven unless the specific editor element exposes and verifies a semantic `set_value` path.

Use one click to focus the editor area before typing. Avoid triple-click replacement patterns until Linux app-specific editing heuristics are richer.
