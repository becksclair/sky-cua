#!/bin/sh

if [ "$#" -eq 1 ] && [ "$1" = "--version" ]; then
  printf '%s\n' 'node_repl-fake/1.0.0'
  exit 0
fi

script_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
exec "$script_dir/node" "$script_dir/../lib/node_repl.mjs" "$@"
