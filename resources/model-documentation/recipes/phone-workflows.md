# Phone workflows

Discover devices, connect explicitly, bind the selected device, and retain the session. Capture a screenshot before coordinate actions. Use the screenshot's bytes or data URL with `nodeRepl.emitImage`. Check advertised capabilities before companion-only operations. Treat disconnect as terminal for the bound session and reconnect explicitly.
