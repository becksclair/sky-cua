interface:
  display_name: "VM Tests"
  short_description: "Run sky-cua testing VM smoke profiles"
  default_prompt: "Use $vm-tests to run the right sky-cua testing VM smoke profile and report the artifact evidence."
