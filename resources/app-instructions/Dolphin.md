## Dolphin Computer Use

Dolphin is usually better for semantic discovery than for semantic activation. Prefer AT-SPI for reading file lists, names, and focus, then use physical input for double-click/open flows until action routing is proven.

When a file row is already focused, avoid redundant clicks that can unexpectedly change selection state.

