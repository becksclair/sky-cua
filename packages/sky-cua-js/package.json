{
  "name": "@heliasar/sky-cua",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Private pure-ESM Computer Use compatibility facade for the sky-cua service.",
  "engines": {
    "node": ">=24"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./phone": {
      "types": "./dist/phone/index.d.ts",
      "import": "./dist/phone/index.js"
    }
  },
  "bin": {
    "sky-cua-acceptance": "./scripts/acceptance-actions.mjs"
  },
  "files": [
    "dist",
    "scripts/acceptance-actions.mjs",
    "README.md",
    "package.json"
  ],
  "scripts": {
    "build": "bun scripts/build.ts",
    "benchmark:screenshot": "bun run build && node --expose-gc scripts/benchmark-screenshot.mjs",
    "benchmark:screenshot:self-test": "bun run build && node --expose-gc scripts/benchmark-screenshot.mjs --self-test",
    "acceptance:actions": "bun run build && node scripts/acceptance-actions.mjs --dry-run",
    "acceptance:actions:live": "bun run build && node scripts/acceptance-actions.mjs --live",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "bun run build && bun test test",
    "pack": "bun run pack:deterministic",
    "pack:deterministic": "bun run build && bun scripts/pack.ts"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
