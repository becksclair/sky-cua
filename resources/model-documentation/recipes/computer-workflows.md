# Computer workflows

Observe before acting, retain the Computer facade across calls, and choose coordinates from the latest screenshot. For image inspection, use WebP by default and `nodeRepl.emitImage`. For a `file://` result, read it with `node:fs/promises`, then emit the bytes. Confirm window actions from the returned structured result.
