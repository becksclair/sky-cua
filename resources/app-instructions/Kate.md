## Kate Computer Use

Kate exposes a fairly decent accessibility tree on KDE. Prefer semantic reads of focused controls and window titles, but treat editor-content changes as keyboard-driven unless a future semantic text lane proves reliable.

Use one click to focus the editor area before typing. Avoid triple-click replacement patterns until Linux app-specific editing heuristics are richer.

